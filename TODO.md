# Portfolio To-Do

## Pending deletion — awaiting confirmation
- [ ] Delete `image_backups_q85/` folder (25.8 MB) — contains the pre-optimization q=85 WebP backups.
      Kept until site images are verified visually. Once confirmed OK, delete this folder.
      Command: `rm -rf image_backups_q85/`
